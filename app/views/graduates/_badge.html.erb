<div>
  <article class="badge sticker printed">
    <% if graduate.checked_in? %>
      <div class="checkedin-banner full"><span class="material-symbols-outlined">verified</span>CHECKED IN</div>
    <% end %>
  <div class="badge-content">
    <h1 class="name">
      <%= graduate.display_preferred_last.to_s.upcase %>, 
      <%= graduate.display_preferred_first.to_s.upcase %>
    </h1>
    <% if graduate.formal_name_differs_from_preferred? %>
      <div class="formal-name">
        <%= graduate.lastname.to_s.upcase %>, <%= graduate.firstname.to_s.upcase %>
      </div>
    <% end %>
    
    <div class="classification">
    <% if graduate.major.present? || graduate.degree_code.present? %>
      <h4 class="major">
        <% if graduate.levelcode.to_s.start_with?("GR") %>
          <span class="material-symbols-outlined">school</span>
          <% if graduate.degree_code.present? && graduate.major.present? %>
            <%= graduate.degree_code %> in <%= graduate.major %>
          <% elsif graduate.major.present? %>
            <%= graduate.major %>
          <% else %>
            <%= graduate.degree_code %>
          <% end %>
        <% else %>
          <%= graduate.major.presence || graduate.degree_code %>
        <% end %>
      </h4>
    <% end %>
    <h5 class="college">
      <span class="material-symbols-outlined"><%= CollegeCodeTranslator.icon(graduate.college1) %></span> <%= CollegeCodeTranslator.translate_full(graduate.college1) %>
    </h5>
    </div>

    <div class="bottom-row flex grow center">
      <% if !graduate.orderid.present? && graduate.height.present? %>
      <div>
        <%# if graduate.orderid.present? %>
        <!--  <h3 class="order"><span class="material-symbols-outlined">order_approve</span> Order</h3> -->
        <%# end %>
        
        <h3 class="height"><span class="material-symbols-outlined">accessibility_new</span> <%= graduate.height %> <label id="height-modal" for="modal_1" class="button">Edit</label></h3>
      </div>
      <% end %>

      <% if !graduate.orderid.present? && graduate.levelcode.to_s.start_with?("GR") && graduate.hoodcolor.present? %>
        <div>
          <h3 class="hood-color"><span class="material-symbols-outlined">palette</span> <%= graduate.hoodcolor %></h3>
        </div>
      <% end %>
      
      <% if graduate.honors == "Y" %>
        <div>
          <h3 class="honors"><span class="material-symbols-outlined">new_releases</span> Hon Cord</h3>
        </div>
      <% end %>
      
      <% if graduate.cords.count > 0 %>
        <div>
          <h3 class="honors"><span class="material-symbols-outlined">digital_wellbeing</span> WC Cord</h3>
        </div>
      <% end %>
      
      <% if graduate.brags.count > 0 %>
        <div>
          <% if graduate.brags.count == 1 %>
            <h3 class="honors">
              <span class="material-symbols-outlined">diagnosis</span> Note
            </h3>
          <% else %>
            <h3 class="honors">
              <span class="material-symbols-outlined">diagnosis</span> Note <small>(<%= graduate.brags.count %>)</small>
            </h3>
          <% end %>
        </div>
      <% end %>
    </div>

  </div>
  <% if graduate.printed? %>
    <div class="printed-badge">
      <span class="material-symbols-outlined">Print</span>
    </div>
  <% end %> 
</article>

<%= render partial: "badge_action_bar", locals: { graduate: graduate, currentpage: currentpage } %>
</div>